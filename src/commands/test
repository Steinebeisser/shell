# 0 "builtins.def"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "builtins.def"
# 14 "builtins.def"
BUILTIN(exit,
        "exit [optional] <uint8>", "Exits the shell",
        ((const char*[]){ "exit 2", NULL})

         ,
        NULL,
        builtin_exit
)

BUILTIN(set,
        "set [optional] (<modifier>) <key> = <value>",
        "Sets a config for current session",
        ((const char*[]){ "set(alias) ls = 'ls -l' -p", "set $ic = red", "set prompt = \"Prompt > \"", NULL})



         ,
        ((const char*[]){ "-p/--persistence: persist to file", "-f/--force: force it", NULL})


         ,
        builtin_set
)
